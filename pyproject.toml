[project]
name = "langchain-course"
version = "0.1.0"
description = "Sujit's first modern-Python project configuration file, toml(Tom's Obvious, Minimal Language)"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "black>=26.5.1",
    "isort>=8.0.1",
    "langchain>=1.3.4",
    "langchain-google-genai>=4.2.4",
    "langchain-openai>=1.2.2",
    "python-dotenv>=1.2.2",
]
